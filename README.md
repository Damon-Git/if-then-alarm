# 急急如律令

《急急如律令》是一款优先适配 macOS 的独立桌面小窗工具。当前阶段先以 Web App 方式实现 MVP，后续再迁移到 Tauri。

产品使用“符箓 + 香 + 香炉”的中国文化视觉隐喻承载执行意图和番茄钟流程。它不宣称宗教、玄学或超自然功效，核心目标是用仪式感帮助用户启动行动。

## 当前范围

当前版本是无动画 MVP，优先跑通完整业务流程：

- 填写 1-3 套执行意图。
- 每套包含 1 条情境性执行意图、0-3 条预防性执行意图、1-3 炷香。
- 情境性执行意图使用“当……就……”轻校验。
- 预防性执行意图使用“如果……那么……”轻校验。
- 进入仪式台后点击情境性符箓开始倒计时。
- 单炷香结束后选择休息或续香。
- 全部完成后填写复盘，记录完成度、主要阻碍和下次调整。
- 复盘写入 localStorage 历史记录。
- 历史记录以折叠摘要展示复盘字段，支持删除单条和清空全部。
- 历史记录顶部展示轻量分析：总轮次、完成度、总香数、累计专注、阻碍 Top 5 和最近下次调整。
- 仪式台和复盘页提供放弃本轮、刷新/关闭保护。
- 未完成轮次会暂存到 localStorage，刷新后可选择恢复或丢弃。

当前不做复杂视觉、动画、音效、真实素材替换、后端和桌面打包。

## 技术栈

- React
- TypeScript
- Vite
- 普通 CSS
- localStorage

## 运行

安装依赖：

```bash
npm install
```

启动开发服务：

```bash
npm run dev
```

构建验证：

```bash
npm run build
```

## 开发计时常量

当前默认使用开发计时，方便测试：

```ts
DEV_TIMER_SECONDS = 10
DEV_BREAK_SECONDS = 5
```

未来正式版可切换为：

```ts
PROD_TIMER_SECONDS = 25 * 60
PROD_BREAK_SECONDS = 5 * 60
```

常量定义在 `src/constants.ts`。

## 文档

- `docs/MVP_SCOPE.md`：当前 MVP 做什么、不做什么。
- `docs/STATE_MACHINE.md`：页面阶段和套组状态流转。
- `docs/FUTURE_VISUAL_SPEC.md`：未来真实素材和动画迭代原则。
- `docs/ACCEPTANCE_CHECKLIST.md`：手动验收清单。

## 协作约定

- 文档使用中文，文件名使用英文。
- 代码文件名使用英文。
- 不自动提交 Git。
- 敏感信息放到 `.env.local`，不要进入 Git。
- 当前阶段以流程完整和状态可靠为第一优先级。
