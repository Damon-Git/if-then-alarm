# MVP 范围

《急急如律令》的第一轮 MVP 目标是跑通从执行意图填写到本地历史保存的完整闭环。当前版本同时保留 Web App 开发入口和 Tauri 桌面入口；Tauri 入口已经具备 macOS 小窗、菜单栏入口、系统通知、桌面 JSON 持久化、系统文件对话框、窗口置顶和 Dock 显示设置。

Tauri 迁移路径见 `docs/TAURI_MIGRATION_PLAN.md`。当前阶段仍不做真实打包发布、签名、公证和正式图标，但桌面外壳已不再只是占位验证。

小窗目标尺寸和验收约束见 `docs/COMPACT_WINDOW_SPEC.md`。Tauri 桌面窗口当前以 `390px × 620px` 为小窗基线，Web 窄窗口仍作为快速调试和自动检查入口。

## 当前要做

- 用户可以创建 1-3 套执行意图。
- 每套执行意图包含 1 条情境性执行意图、0-3 条预防性执行意图、1-3 炷香。
- 情境性执行意图使用“当……就……”句式，并做轻校验、空白归一化和字段级错误定位。
- 预防性执行意图使用“如果……那么……”句式；未填写时跳过校验，填写后做空白归一化和字段级错误定位。
- 提交后进入仪式台，显示情境性符箓占位、香炉占位、预防性符箓占位。
- 点击情境性符箓后先确认本套内容，再开始当前炷香倒计时。
- 单炷香结束后弹出休息或续香选择。
- 休息结束后提醒继续下一炷香。
- 全部香完成后进入复盘流程，可回看本轮详情，并记录完成度、主要阻碍、下次调整和一句复盘。
- Web 环境下复盘保存到 localStorage；Tauri 环境下复盘保存到 app data 目录下的桌面 JSON。
- 刷新页面或重启桌面应用后仍能查看历史记录。
- 历史记录以折叠摘要展示复盘字段，支持删除单条和清空全部。
- 历史记录顶部展示轻量分析：总轮次、完成度、总香数、累计专注、阻碍 Top 5 和最近下次调整。
- 历史记录支持搜索、完成度筛选和时间范围筛选，分析面板会跟随筛选结果更新。
- 历史记录支持导出 JSON 备份，并支持导入本应用导出的 JSON 文件。
- 复盘保存、历史导入导出、删除和清空使用轻量 toast 提示。
- 支持开发模式和正式模式切换，计时模式保存到 localStorage，并写入当前 session 和历史记录。
- 历史和设置收为右上角弱入口，默认不挤占主流程。
- 窄窗口下仪式台切换为小窗香炉舞台，只显示用户创建的 1-3 个香炉槽位。
- 小窗香炉槽位可作为未开始套组的交互入口，点击后进入同一套开始确认流程。
- 小窗下弱化大标题、英文标识和次要操作，香炉槽位展示当前主状态文案。
- 小窗下开始确认、休息/续香弹窗使用更紧凑的布局。
- 主祭台和小窗香炉统一通过 `CenserVisual` 渲染，为未来素材替换预留单一入口。
- 情境性和预防性符箓统一通过 `TalismanVisual` 渲染，为未来符箓模板替换预留单一入口。
- 线香统一通过 `IncenseVisual` 渲染，并接收由倒计时计算出的当前炷香进度。
- 视觉组件通过 `src/lib/visualState.ts` 接收语义化视觉状态，业务状态不直接绑定素材状态。
- 视觉素材目录和插槽通过 `src/lib/visualAssets.ts` 约定，当前只预留接入位置。
- 仪式台和复盘页提供“放弃本轮”保护入口。
- 仪式台和复盘页刷新/关闭页面前给出浏览器提示。
- 填写页已有草稿时，刷新/关闭页面前给出浏览器提示。
- 当前轮次在 Web 环境下暂存到 localStorage，在 Tauri 环境下暂存到桌面 JSON，刷新或重启后提示恢复或丢弃。
- 开发环境设置面板提供恢复场景 fixture，用于手动验收刷新恢复边界。
- 历史记录当前只用于回看、分析和反思，不提供一键复用。
- 提供 Tauri 桌面外壳，窗口尺寸按 `390px × 620px` 小窗目标验证。
- Tauri 桌面版提供菜单栏“令”入口，用于隐藏和重新打开窗口。
- Tauri 桌面版提供系统通知，用于专注和休息计时结束提醒。
- Tauri 桌面版通过系统文件对话框导入和导出历史 JSON。
- Tauri 桌面版提供“窗口置顶”和“在 Dock 中显示”设置。
- Tauri 桌面配置通过 `npm run check:desktop-config` 做静态回归检查。

## 当前不做

- 不做香炉开盖动画。
- 不做符箓燃烧动画。
- 不做线香燃烧动画。
- 不做烟雾动画。
- 不做复杂音效。
- 不接后端。
- 不做真实桌面打包。
- 不做真实 macOS 打包、签名、公证和发布。
- 不接全局快捷键。
- 不做云同步。
- 不重新绘制复杂符箓、香炉、线香。
- 不制作最终 Q 版香炉素材；当前小窗香炉只表达槽位结构。
- 不导入真实视觉素材；当前只建立素材接入管线。
- 不制作正式应用图标、菜单栏 template icon 或通知图标。
- 不在正式构建中显示恢复场景 fixture。
- 不宣称宗教、玄学或超自然功效。

## 当前计时策略

倒计时以时间戳为准。运行中保存当前计时段的 `startedAt` 和 `durationSeconds`，刷新恢复时根据真实经过时间重新计算剩余秒数。旧版本暂存数据没有计时段字段时，恢复后会用保存的剩余秒数重新建立计时段。

## 第一优先级

当前阶段以完整业务流程和状态可靠性为第一优先级。视觉只使用清晰的占位元素，保证未来可以替换为真实符箓、香炉、线香素材。

## 书写原则

执行意图不是模板填充，而是用户重新确认行动承诺的过程。当前阶段保留手写或打字的摩擦感，不从历史记录一键复用旧意图；历史记录先服务于回看、分析和反思。
